# Pixel Playground V22 QA

- Fixed startup JavaScript exception caused by `ui.home` referencing a non-existent element.
- Restored execution of `buildLists()`, so the solo game list and hub game picker populate.
- Preserved V21 requested layout/mechanics.
- `node --check` passes for inline JavaScript.
- Verified 17 games remain configured.
